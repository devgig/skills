# Backstage descriptor — https://backstage.io/docs/features/software-catalog/descriptor-format
#
# Generated by scaffold-csharp-microservice from the service's spec.md.
# Hand-edits will be overwritten on the next skill run — edit the spec instead.

apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: {{SERVICE_NAME_KEBAB}}
  title: {{SERVICE_NAME}}
  description: {{SERVICE_DESCRIPTION}}
  annotations:
    backstage.io/techdocs-ref: dir:.
  tags:
    - dotnet
    - microservice
    - api
    - mongodb
    - opentelemetry
spec:
  type: service
  lifecycle: {{LIFECYCLE}}
  owner: {{OWNER}}
  system: {{SYSTEM}}
  providesApis:
    - {{SERVICE_NAME_KEBAB}}-api
  dependsOn:
    - resource:mongodb

---

apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: {{SERVICE_NAME_KEBAB}}-api
  title: {{SERVICE_NAME}} API
  description: RESTful API surface for {{SERVICE_NAME}}.
spec:
  type: openapi
  lifecycle: {{LIFECYCLE}}
  owner: {{OWNER}}
  system: {{SYSTEM}}
  definition:
    $text: ./specs/001-initial-service/contracts/{{RESOURCE_SLUG}}-api.yaml

---

apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: mongodb
  title: MongoDB Database
  description: MongoDB database backing {{SERVICE_NAME}}.
spec:
  type: database
  owner: {{OWNER}}
  system: {{SYSTEM}}
